{
  "name": "link-to-pdf-cli",
  "version": "1.0.1",
  "description": "Convert URLs to PDF from CLI",
  "main": "dist/index.js",
  "type": "module",
  "bin": {
    "link-to-pdf-cli": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "prepare": "npm run build"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "puppeteer": "^22.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}